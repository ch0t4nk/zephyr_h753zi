# direnv configuration for Zephyr repo: source the zephyr-env.sh provided in the repo
# To enable, run: direnv allow
if [ -f "$(pwd)/zephyr/zephyr-env.sh" ]; then
    source "$(pwd)/zephyr/zephyr-env.sh"
fi
