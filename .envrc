# direnv configuration for workspace: automatically load Zephyr environment
# This file is safe to review. To enable it, run: direnv allow

# Source the repo-provided zephyr environment (if present)
if [ -f "$HOME/zephyr-dev/zephyr/zephyr/zephyr-env.sh" ]; then
    source "$HOME/zephyr-dev/zephyr/zephyr/zephyr-env.sh"
fi
